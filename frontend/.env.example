# Main Strategy Screener API (or leave unset in dev to use setupProxy.js → localhost:8000)
REACT_APP_API_URL=http://localhost:8000

# Trading service (Backtest replay, Trading page, some Admin actions). Not proxied — set when trading-service runs.
REACT_APP_TRADING_API_URL=http://localhost:8001
